# =============================================================================
# LiveKit Protocol Agent - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your credentials:
#   cp .env.protocol .env
#   nano .env
# =============================================================================

# -----------------------------------------------------------------------------
# LiveKit Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Get these from https://cloud.livekit.io -> Your Project -> Settings -> Keys

LIVEKIT_URL="wss://your-project.livekit.cloud"
LIVEKIT_API_KEY="your_api_key_here"
LIVEKIT_API_SECRET="your_api_secret_here"

# -----------------------------------------------------------------------------
# Speech-to-Text Provider API Keys
# -----------------------------------------------------------------------------
# Uncomment and fill in the API key for your chosen provider

# Deepgram (default, recommended)
# Get your API key from https://console.deepgram.com/
DEEPGRAM_API_KEY="your_deepgram_api_key_here"

# Speechmatics (alternative, 55+ languages)
# Get your API key from https://portal.speechmatics.com/
# SPEECHMATICS_API_KEY="your_speechmatics_api_key_here"

# OpenAI Whisper (alternative)
# Get your API key from https://platform.openai.com/
# OPENAI_API_KEY="your_openai_api_key_here"

# -----------------------------------------------------------------------------
# Agent Configuration (OPTIONAL - defaults shown)
# -----------------------------------------------------------------------------

# Speech-to-text provider: deepgram, speechmatics, or openai
STT_PROVIDER="deepgram"

# Output format: txt, json, or both
OUTPUT_FORMAT="both"

# Directory for protocol output files
PROTOCOLS_DIR="protocols"

# Enable statistics tracking (word count, turns per participant)
ENABLE_STATISTICS="true"

# -----------------------------------------------------------------------------
# Advanced Configuration (OPTIONAL)
# -----------------------------------------------------------------------------

# Log level: debug, info, warning, error
# LIVEKIT_LOG_LEVEL="info"

# Filter participants (comma-separated list, empty = all participants)
# ALLOWED_PARTICIPANTS=""

# Generate AI summary at end of meeting (requires OPENAI_API_KEY)
# GENERATE_SUMMARY="false"
